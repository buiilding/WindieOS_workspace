#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ -x "${ROOT_DIR}/bin/docs-list" ]]; then
  exec "${ROOT_DIR}/bin/docs-list"
fi

exec node "${ROOT_DIR}/scripts/docs-list.ts"
